import { VotingCampaignCalculator } from "./shared";

export const calculateRandomSelectResults: VotingCampaignCalculator = async (meta) => {
	return { error: "calculationError", details: "meowwww" };
};
